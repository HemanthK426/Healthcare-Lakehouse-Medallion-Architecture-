from pyspark.sql import SparkSession

spark = SparkSession.builder.appName("BronzeLayer").getOrCreate()

df = spark.read.csv("data/raw/patient_data.csv", header=True, inferSchema=True)

df.write.format("delta").mode("overwrite").save("data/bronze/patient")
